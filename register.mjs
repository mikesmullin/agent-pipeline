// pipeline/register.mjs
// Convenience preload so a consuming project (or the CLI) can run .coffee files.
// Equivalent to adding `preload = ["bun-coffeescript/register"]` to bunfig.toml.
import 'bun-coffeescript/register'
